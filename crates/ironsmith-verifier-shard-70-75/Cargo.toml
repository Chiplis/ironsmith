[package]
name = "ironsmith-verifier-shard-70-75"
version = "0.1.0"
edition = "2024"

[dependencies]
ironsmith-verifier-ziffle = { path = "../ironsmith-verifier-ziffle", default-features = false }

[features]
default = []
