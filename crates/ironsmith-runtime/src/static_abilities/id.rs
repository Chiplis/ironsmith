//! Static ability identity enum.
//!
//! This shared identity data is owned by `ironsmith-core` and re-exported here
//! so runtime can keep its existing module path during the extraction.

pub use ironsmith_core::StaticAbilityId;
