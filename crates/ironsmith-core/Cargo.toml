[package]
name = "ironsmith-core"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
ironsmith-tag-walk-derive = { path = "../ironsmith-tag-walk-derive" }
serde = { version = "1.0.228", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
